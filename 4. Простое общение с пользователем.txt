¬с€ информаци€ которую вводит пользователь всегда приходит в формате строки(string)

„тобы перевести из строчного формата в числовой то что вводит пользователь надо перед prompt поставить знак +

„“обы корректно работал prompt в IE,да и в целом более правильно считаетс€ оставл€ть пустую строку после вопроса/текста. ѕример: const answer = prompt("¬ам есть 18?","");

„“обы узнать какой тип данных мы получили/имеем есть команда typeoff

ѕризнанна€ ошибка в js - если следать typeoff у null, то покажет что он €вл€етс€ обьектом, хот€ это чушь)

≈сть вещи которые работают только в браузере и поэтому если мы захотим запустить код с prompt,confirm,alert,document,window в консоле, то у нас ничего не выйдет и будет ошибка так как консоль не знает о существовании такой команды(у нее и модальных окон то нет). Ќо при этом в браузере все будет прекрасно работать и это будет правильно.